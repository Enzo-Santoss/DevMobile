// tiny placeholder so Stencil's dynamic import glob is not empty
// This prevents the "empty-glob" webpack/vite warning during dev builds.
// The file is intentionally minimal and safe.
export default {};
